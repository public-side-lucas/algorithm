---
title: "삼각형 외우기"
date: 2026-02-22
category: 기타
difficulty: easy
---

# 삼각형 외우기

| 항목 | 내용 |
|------|------|
| 📅 작성일 | 2026-02-22 |
| 📁 카테고리 | 기타 |
| 🎯 난이도 | 🟢 쉬움 |
| ⏱️ 시간 복잡도 | **O(n)** |
| 💾 공간 복잡도 | **O(n)** |

## 📝 개요

삼각형 외우기 성공다국어
시간 제한	메모리 제한	제출	정답	맞힌 사람	정답 비율
1 초	256 MB	65304	35808	32907	56.602%
문제
창영이는 삼각형의 종류를 잘 구분하지 못한다. 따라서 프로그램을 이용해 이를 외우려고 한다.

삼각형의 세 각을 입력받은 다음,

세 각의 크기가 모두 60이면, Equilateral
세 각의 합이 180이고, 두 각이 같은 경우에는 Isosceles
세 각의 합이 180이고, 같은 각이 없는 경우에는 Scalene
세 각의 합이 180이 아닌 경우에는 Error
를 출력하는 프로그램을 작성하시오.

입력
총 3개의 줄에 걸쳐 삼각형의 각의 크기가 주어진다. 모든 정수는 0보다 크고, 180보다 작다.

출력
문제의 설명에 따라 Equilateral, Isosceles, Scalene, Error 중 하나를 출력한다.

예제 입력 1 
60
70
50
예제 출력 1 
Scalene

## 💻 알고리즘 코드

```javascript
// 백준 스타일 입력 — require("fs").readFileSync 지원
const input = require("fs").readFileSync("/dev/stdin", "utf8").trim();
const [x,y,z] = input.split("\n").map(Number);

// 세 각의 크기가 모두 60이면, Equilateral
// 세 각의 합이 180이고, 두 각이 같은 경우에는 Isosceles
// 세 각의 합이 180이고, 같은 각이 없는 경우에는 Scalene
// 세 각의 합이 180이 아닌 경우에는 Error
const sum = x+y+z
if (sum !== 180) {
  console.log("Error");
} else {
  // Set에 세 각을 넣으면 중복이 제거됩니다.
  const angleSet = new Set([x, y, z]);
  const uniqueCount = angleSet.size;

  if (uniqueCount === 1) {
    // 모든 각이 같음 (당연히 60도씩이겠죠?)
    console.log("Equilateral");
  } else if (uniqueCount === 2) {
    // 두 각만 같음
    console.log("Isosceles");
  } else {
    // 모두 다름
    console.log("Scalene");
  }
}
```

## 📥 입력과 출력

### 입력

```
60
70
50
```

### 출력

```
"Scalene"
```

## 📊 복잡도 분석

| 구분 | 복잡도 |
|------|--------|
| ⏱️ 시간 복잡도 | **O(n)** |
| 💾 공간 복잡도 | **O(n)** |

> 빌트인 메서드 2개 감지. 시간 복잡도 O(n)(선형 시간 — 입력에 비례), 공간 복잡도 O(n).

#### 상세 분석

- 📦 **Line 3** `input.split("\n").map(Number)` — Array.map()는 O(n)
- 📦 **Line 3** `input.split("\n")` — String.split()는 O(n)

## 🔍 실행 흐름 분석

총 **9개**의 실행 단계가 기록되었습니다.

| Step | Line | input | x | y | z | sum | angleSet | uniqueCount | 설명 |
|------|------|------|------|------|------|------|------|------|------|
| 1 | 2 | "60
70
50" | - | - | - | - | - | - | const input = "60
70
50" |
| 2 | 3 | "60
70
50" | 60 | 70 | 50 | - | - | - | const destructured = [60, 70, 50] |
| 3 | 9 | "60
70
50" | 60 | 70 | 50 | 180 | - | - | const sum = 180 |
| 4 | 10 | "60
70
50" | 60 | 70 | 50 | 180 | - | - | if (if (sum !== 180) {) → false |
| 5 | 14 | "60
70
50" | 60 | 70 | 50 | 180 | undefined | - | const angleSet = undefined |
| 6 | 15 | "60
70
50" | 60 | 70 | 50 | 180 | undefined | undefined | const uniqueCount = undefined |
| 7 | 17 | "60
70
50" | 60 | 70 | 50 | 180 | undefined | undefined | if (if (uniqueCount === 1) {) → false |
| 8 | 20 | "60
70
50" | 60 | 70 | 50 | 180 | undefined | undefined | if (} else if (uniqueCount === 2) {) → false |
| 9 | 25 | "60
70
50" | 60 | 70 | 50 | 180 | undefined | undefined | console.log("Scalene");() called |

## 💡 핵심 포인트

Set의 활용

---

> 🤖 이 포스트는 **Algorithm Flow** 시각화 도구를 통해 자동으로 생성되었습니다.
> 📅 생성 일시: 2026. 2. 22. 오전 9:34:51
