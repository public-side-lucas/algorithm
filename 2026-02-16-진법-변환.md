---
title: "진법 변환"
date: 2026-02-16
category: 기타
difficulty: easy
tags:
  - 진법 변환
  - 진법
  - 호너의법칙
---

# 진법 변환

| 항목 | 내용 |
|------|------|
| 📅 작성일 | 2026-02-16 |
| 📁 카테고리 | 기타 |
| 🎯 난이도 | 🟢 쉬움 |
| ⏱️ 시간 복잡도 | **O(n)** |
| 💾 공간 복잡도 | **O(n)** |
| 🏷️ 태그 | `진법 변환`, `진법`, `호너의법칙` |

## 📝 개요

진법 변환 성공
시간 제한	메모리 제한	제출	정답	맞힌 사람	정답 비율
1 초	128 MB	92385	45792	38528	49.031%
문제
B진법 수 N이 주어진다. 이 수를 10진법으로 바꿔 출력하는 프로그램을 작성하시오.

10진법을 넘어가는 진법은 숫자로 표시할 수 없는 자리가 있다. 이런 경우에는 다음과 같이 알파벳 대문자를 사용한다.

A: 10, B: 11, ..., F: 15, ..., Y: 34, Z: 35

입력
첫째 줄에 N과 B가 주어진다. (2 ≤ B ≤ 36)

B진법 수 N을 10진법으로 바꾸면, 항상 10억보다 작거나 같다.

출력
첫째 줄에 B진법 수 N을 10진법으로 출력한다.

## 💻 알고리즘 코드

```javascript
// 버블 정렬 알고리즘 예시
// Input에 정렬할 숫자를 한 줄씩 입력하세요
const input = readline();
const inputs = input.split(' ')

const B = Number(inputs[1])
const N = inputs[0].split("")

function solve(B, N ) {
return N.reduce((acc, cur) => acc * B + parseInt(cur, 36), 0);
}

console.log(solve(B,N))
```

## 📥 입력과 출력

### 입력

```
ZZZZZ 36
```

### 출력

```
60466175
```

## 📊 복잡도 분석

| 구분 | 복잡도 |
|------|--------|
| ⏱️ 시간 복잡도 | **O(n)** |
| 💾 공간 복잡도 | **O(n)** |

> 빌트인 메서드 3개 감지. 시간 복잡도 O(n)(선형 시간 — 입력에 비례), 공간 복잡도 O(n).

#### 상세 분석

- 📦 **Line 4** `input.split(' ')` — String.split()는 O(n)
- 📦 **Line 7** `inputs[0].split("")` — String.split()는 O(n)
- 📦 **Line 10** `N.reduce((acc, cur) => acc * B + parseInt(cur, 36)…` — Array.reduce()는 O(n)

## 🔍 실행 흐름 분석

총 **7개**의 실행 단계가 기록되었습니다.

| Step | Line | input | inputs | B | N | solve | 설명 |
|------|------|------|------|------|------|------|------|
| 1 | 3 | "ZZZZZ 36" | - | - | - | - | const input = "ZZZZZ 36" |
| 2 | 4 | "ZZZZZ 36" | ["ZZZZZ", "36"] | - | - | - | const inputs = ["ZZZZZ", "36"] |
| 3 | 6 | "ZZZZZ 36" | ["ZZZZZ", "36"] | 36 | - | - | const B = 36 |
| 4 | 7 | "ZZZZZ 36" | ["ZZZZZ", "36"] | 36 | ["Z", "Z", "Z", "Z", "Z"] | - | const N = ["Z", "Z", "Z", "Z", "Z"] |
| 5 | 9 | "ZZZZZ 36" | ["ZZZZZ", "36"] | 36 | ["Z", "Z", "Z", "Z", "Z"] | [Function] | function solve() declared |
| 6 | 10 | "ZZZZZ 36" | ["ZZZZZ", "36"] | 36 | ["Z", "Z", "Z", "Z", "Z"] | [Function] | return 60466175 |
| 7 | 13 | "ZZZZZ 36" | ["ZZZZZ", "36"] | 36 | ["Z", "Z", "Z", "Z", "Z"] | [Function] | console.log(solve(B,N))() called |

## 💡 핵심 포인트

호너의 법칙을 참고(https://en.wikipedia.org/wiki/Horner%27s_method)

---

> 🤖 이 포스트는 **Algorithm Flow** 시각화 도구를 통해 자동으로 생성되었습니다.
> 📅 생성 일시: 2026. 2. 16. 오후 2:13:07
